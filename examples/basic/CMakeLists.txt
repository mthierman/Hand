if(PROJECT_IS_TOP_LEVEL)
    hand_add_plugin(
        ID
        "com.examples.basic"
        NAME
        "BasicPlugin"
        VENDOR
        "Examples"
        SOURCES
        "plugin.cxx"
        FEATURES
        "audio-effect"
        )
endif()
