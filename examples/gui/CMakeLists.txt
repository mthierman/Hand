if(PROJECT_IS_TOP_LEVEL)
    hand_plugin(
        ID
        "com.examples.gui"
        NAME
        "GuiPlugin"
        VENDOR
        "Examples"
        SOURCES
        "plugin.cxx"
        )
endif()
