if(PROJECT_IS_TOP_LEVEL)
    hand_add_plugin(
        ID
        "com.examples.gui"
        NAME
        "GuiPlugin"
        VENDOR
        "Examples"
        SOURCES
        "plugin.cxx"
        FEATURES
        "audio-effect"
        )
endif()
